// itemDetails = new ItemDetails();
					// itemDetails.setProductIdList(productIdList);
					// itemDetails.setSampleId(sampleDesignCostSheet.getId());
					// ejb.setItemDetails(itemDetails);
					//
					// String string = itemDetails.getProductIdList();
					// String[] parts = string.split(" ");
					// for (String str : parts) {
					// System.out
					// .println(ejb.getProductDetailById(Integer.parseInt(str)).getDescription()
					// + "," + str);
					// }